% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/options.R
\name{GridOptions}
\alias{GridOptions}
\title{Grid search options for \link{predict_grid}.}
\usage{
GridOptions(
  threshold = c(0, 0.2, 0.5, 0.8),
  censor_type = "both",
  censor_unit = "percent",
  censor_operator = "greater_than_or_equal_to",
  prediction_scale = "response",
  adj_fit_multiplier = "k",
  inv_method = "gaussian",
  include_linear_regression = FALSE,
  verbose = FALSE,
  max_iter = 1000L,
  k = 1L,
  seed = 42L,
  retain_all = FALSE,
  retain_grid_objects = NULL,
  attribute_combi = NULL,
  adjust_impact_for_missing = TRUE,
  inner_parallel = "auto"
)
}
\arguments{
\item{threshold}{Numeric vector of thresholds. Default \code{c(0, 0.2, 0.5, 0.8)}.}

\item{censor_type}{\code{"relevance"}, \code{"similarity"}, or \code{"both"} (default).}

\item{censor_unit}{\code{"percent"} or \code{"score"}.}

\item{censor_operator}{Comparison vs threshold. Default \code{"greater_than_or_equal_to"}.}

\item{prediction_scale}{\code{"response"} or \code{"logistic"}.}

\item{adj_fit_multiplier}{\code{"k"}, \code{"identity"}, or \code{"log"}.}

\item{inv_method}{\code{"gaussian"}, \code{"cholesky"}, or \code{"pseudoinverse"}.}

\item{include_linear_regression}{Logical; fills \code{yhat_linear} when available.}

\item{verbose}{Logical; engine diagnostics (default \code{FALSE}).}

\item{max_iter}{Combination search budget.}

\item{k}{Combination size.}

\item{seed}{RNG seed.}

\item{retain_all}{Retain all grid cell objects.}

\item{retain_grid_objects}{Character policy string (overrides \code{retain_all}).}

\item{attribute_combi}{Optional matrix of attribute combinations.}

\item{adjust_impact_for_missing}{Logical; incomplete-column IOF/IOP vs a
0/1 include-k null (default \code{TRUE}). \code{FALSE} is the pre-adjustment /
PSR-parity baseline.}

\item{inner_parallel}{\code{"auto"} or \code{"off"} (within-call parallelism).}
}
\value{
A named list with class \code{GridOptions}.
}
\description{
Same censor / scale / inversion knobs as \link{PredictOptions}, plus combination
search settings. Does \strong{not} take \code{verify_missing_data} / \code{missing_moments}:
Grid chooses complete vs pairwise moments per combination.
}
\examples{
GridOptions(k = 2L, max_iter = 100L)

}
\seealso{
\link{PredictOptions}, \link{predict_grid}
}
