% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/options.R
\name{PredictOptions}
\alias{PredictOptions}
\alias{MaxFitOptions}
\title{Configuration objects (named lists) for predict / maxfit / grid.}
\usage{
PredictOptions(
  threshold = NULL,
  censor_type = "relevance",
  censor_unit = "percent",
  censor_operator = "greater_than_or_equal_to",
  prediction_scale = "response",
  adj_fit_multiplier = "k",
  inv_method = "gaussian",
  verify_missing_data = FALSE,
  include_linear_regression = FALSE,
  verbose = FALSE
)

MaxFitOptions(
  threshold = c(0, 0.2, 0.5, 0.8),
  censor_type = "both",
  censor_unit = "percent",
  censor_operator = "greater_than_or_equal_to",
  prediction_scale = "response",
  adj_fit_multiplier = "k",
  inv_method = "gaussian",
  verify_missing_data = FALSE,
  include_linear_regression = FALSE,
  verbose = FALSE,
  objective = "kfit",
  inner_parallel = "auto"
)
}
\arguments{
\item{threshold}{Numeric vector of thresholds, or \code{NULL} for engine default.}

\item{censor_type}{Character: \code{"relevance"}, \code{"similarity"}, or \code{"both"}
(\code{"both"} only for maxfit/grid).}

\item{censor_unit}{\code{"percent"} or \code{"score"}.}

\item{censor_operator}{Comparison vs threshold. Default \code{"greater_than_or_equal_to"} (\code{">="}).}

\item{prediction_scale}{\code{"response"} or \code{"logistic"}.}

\item{adj_fit_multiplier}{\code{"k"}, \code{"identity"}, or \code{"log"}.}

\item{inv_method}{\code{"gaussian"}, \code{"cholesky"}, or \code{"pseudoinverse"}.}

\item{verify_missing_data}{Logical. Predict / MaxFit only (not \link{GridOptions}).}

\item{include_linear_regression}{Logical; fills \code{yhat_linear} when available.}

\item{verbose}{Logical; when \code{TRUE}, the engine may print library status
diagnostics (default \code{FALSE}).}

\item{objective}{MaxFit objective: \code{"kfit"}, \code{"fit"}, or \code{"adjusted_fit"}.}

\item{inner_parallel}{\code{"auto"} or \code{"off"} (within-call parallelism).}
}
\value{
A named list with class \code{PredictOptions} or \code{MaxFitOptions}.
}
\description{
Mirrors Python \code{PredictOptions}, \code{MaxFitOptions}, and \code{GridOptions}.
Pass the list to \link{predict_rbp}, \link{predict_maxfit}, or \link{predict_grid}.
Fields set to \code{NULL} are omitted so the engine applies its defaults.
}
\examples{
PredictOptions(threshold = 0.5)
MaxFitOptions(objective = "kfit")

}
\seealso{
\link{GridOptions}, \link{predict_rbp}, \link{predict_maxfit}
}
