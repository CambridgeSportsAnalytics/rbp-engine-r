% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/native.R
\name{engine}
\alias{engine}
\alias{engine_candidate_paths}
\alias{engine_available}
\alias{find_engine_path}
\title{Locate and load the private RBP Math Engine runtime.}
\usage{
engine_candidate_paths()

engine_available()

find_engine_path()
}
\value{
\code{\link[=engine_available]{engine_available()}} returns a logical. \code{\link[=engine_candidate_paths]{engine_candidate_paths()}}
returns character paths that may be probed. \code{\link[=find_engine_path]{find_engine_path()}} and
\code{\link[=engine_load]{engine_load()}} return the loaded path (invisibly). \code{\link[=ensure_engine]{ensure_engine()}}
returns \code{TRUE} invisibly when the engine is available.
}
\description{
The CRAN/open \strong{rbpengine} package is a thin client. Predictions require the
private \strong{RBP Math Engine} shared library (\code{librbp_math_lib.dylib} /
\code{.so} / \code{rbp_math_lib.dll}) plus platform companions (OpenBLAS/LAPACK where
needed) and a valid license.
}
\details{
Discovery order (first path that loads and exposes the expected ABI symbols):
\enumerate{
\item \code{RBP_ENGINE_LIB} (full path to the shared library)
\item \code{RBP_ENGINE_HOME} / \verb{lib/} (+ OS filename)
\item \code{\link[=install_engine]{install_engine()}} prefix (\code{\link[=default_engine_home]{default_engine_home()}})
\item Well-known install prefixes (\verb{/Library/rbp-engine/lib},
\verb{$HOME/.local/share/rbp-engine/lib}, ...)
\item Developer cargo tree next to this package (\code{../target/release} or
\code{../target/debug})
}
}
\examples{
engine_available()
length(engine_candidate_paths()) >= 1
}
\seealso{
\code{\link[=library_path]{library_path()}}, \code{\link[=abi_version]{abi_version()}}, \code{\link[=install_engine]{install_engine()}}
}
