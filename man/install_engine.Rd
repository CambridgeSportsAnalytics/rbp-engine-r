% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install_engine.R
\name{install_engine}
\alias{install_engine}
\title{Install the private RBP Math Engine runtime.}
\usage{
install_engine(
  version = "latest",
  dest = NULL,
  url = NULL,
  path = NULL,
  load = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{version}{Release to fetch. \code{"latest"} (default) or a semver such as
\code{"1.3.0"}. Ignored when \code{path} or \code{url} is set.}

\item{dest}{Install prefix. Default is \code{\link[=default_engine_home]{default_engine_home()}}
(\verb{~/.local/share/rbp-engine} on Unix, \verb{\%LOCALAPPDATA\%/rbp-engine} on
Windows).}

\item{url}{Full URL of a \code{.tar.gz} (skips the manifest). \code{sha256} is not
verified unless \code{manifest_url} is also reachable via \code{version}.}

\item{path}{Local \code{.tar.gz} or an already-staged runtime directory (\verb{lib/},
\verb{bin/}).}

\item{load}{If \code{TRUE} (default), \code{\link[=engine_load]{engine_load()}} the newly installed library.}

\item{quiet}{Suppress download progress.}
}
\value{
The install prefix, invisibly.
}
\description{
The CRAN/open \strong{rbpengine} package is a thin client. Predictions need the
private engine (shared library + companions + \code{rbp-license-info}). This
helper fetches the platform tarball from
\verb{https://docs.csanalytics.io/releases/} (or unpacks a local archive) into a
well-known prefix that \code{\link[=engine_candidate_paths]{engine_candidate_paths()}} already searches.
}
\details{
This is a \strong{user-called} step, never run at package install time (CRAN
policy). Analogous to \verb{pip install rbp-engine} for the engine files only.
}
\examples{
\dontrun{
install_engine()                 # latest for this machine
install_engine(version = "1.3.0")
install_engine(path = "macos-arm64.tar.gz")
}

}
\seealso{
\code{\link[=ensure_engine]{ensure_engine()}}, \code{\link[=engine_available]{engine_available()}}, \code{\link[=uninstall_engine]{uninstall_engine()}}
}
