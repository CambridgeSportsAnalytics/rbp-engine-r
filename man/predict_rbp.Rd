% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{predict_rbp}
\alias{predict_rbp}
\alias{predict_maxfit}
\alias{predict_grid}
\title{Relevance-Based Prediction entry points.}
\usage{
predict_rbp(y, X, theta, options = NULL)

predict_maxfit(y, X, theta, options = NULL)

predict_grid(y, X, theta, options = NULL)
}
\arguments{
\item{y}{Numeric outcomes, length N.}

\item{X}{Numeric matrix, N rows by K columns (R column-major is native).}

\item{theta}{Circumstances vector, length K.}

\item{options}{Optional \link{PredictOptions}, \link{MaxFitOptions}, or \link{GridOptions}.}
}
\value{
A nested list of class \code{PredictionResults} (same field layout as
Python \code{rbp_engine.PredictionResults}). Access fields with \code{$}, e.g.
\code{result$yhat}, \code{result$insights$relevance}.
}
\description{
Named \code{predict_rbp} (not \code{predict}) so base R's \link[stats:predict]{stats::predict} is not masked.
}
\examples{
\dontrun{
set.seed(1)
N <- 40; K <- 3
X <- matrix(rnorm(N * K), N, K)
y <- rnorm(N)
theta <- colMeans(X)
res <- predict_rbp(y, X, theta, PredictOptions(threshold = 0.5))
res$yhat
best <- predict_maxfit(y, X, theta)
out <- predict_grid(y, X, theta, GridOptions(k = 1L, max_iter = 50L))
as.numeric(out$yhat[1])
}

}
